import numpy as np


def process_video(frames):
    processed_frames = np.copy(frames)
    return processed_frames


def filter_ground_truth(frames):
    filtered_frames = np.copy(frames)
    return filtered_frames
